---
description: Deprecated — renamed to `/dashboard`. Kept as a thin alias so existing invocations and schedules keep working.
---

# /nucleus-dashboard (deprecated)

This command was renamed to `/dashboard` as part of the 2026-09-15 `core-ops` → `ops` plugin rename.

Say once: "`/nucleus-dashboard` has been renamed to `/dashboard` — routing you there now."

Then run `commands/dashboard.md` exactly as if the user had typed `/dashboard`, with no other behavior changes.
