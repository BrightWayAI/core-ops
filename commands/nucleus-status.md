---
description: Deprecated — renamed to `/status`. Kept as a thin alias so existing invocations and schedules keep working.
---

# /nucleus-status (deprecated)

This command was renamed to `/status` as part of the 2026-09-15 `core-ops` → `ops` plugin rename.

Say once: "`/nucleus-status` has been renamed to `/status` — routing you there now."

Then run `commands/status.md` exactly as if the user had typed `/status`, with no other behavior changes.
