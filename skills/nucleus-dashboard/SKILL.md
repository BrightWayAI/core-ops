---
disable-model-invocation: true
name: nucleus-dashboard
description: Deprecated alias for `/dashboard` (renamed 2026-09-15). Only fires on explicit `/nucleus-dashboard` invocation, not on natural-language matching — use `dashboard` for that.
---

This skill was renamed to `dashboard`. See `skills/dashboard/SKILL.md` and `commands/dashboard.md`
for the current workflow.

On explicit `/nucleus-dashboard` invocation: say once "`/nucleus-dashboard` has been renamed to
`/dashboard` — routing you there now," then run the `dashboard` skill's workflow unchanged.
